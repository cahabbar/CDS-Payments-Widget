{{yield 
    configs
    Visa
    MasterCard
    AmericanExpress
    DiscoverCard
    DinersClub
    JCB
    cardToggle
    CVC
    nameOnCard
    wallet
    billMeToggle
    billMe
    AlternativePayments
    savedConfigs
    selectedConfig
    newConfigName
    widgetToggle

    filter
    (action "filterWith" )
    (action "myAction")
    (action "toggle")
    (action "setSelectedConfig")
    (action "saveNewConfig")
    (action "saveNewConfigName")

}}
