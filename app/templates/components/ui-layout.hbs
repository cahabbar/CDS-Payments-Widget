{{yield 
    configs
    Visa
    MasterCard
    AmericanExpress
    DiscoverCard
    DinnersClub
    JCB
    cardToggle
    CVC
    nameOnCard
    wallet
    billMeToggle
    billMe
    payWith
    savedConfigs
    selectedConfig
    newConfigName
    widgetToggle

    filter
    (action "filterWith" )
    (action "myAction")
    (action "toggle")
    (action "setSelectedConfig")
    (action "saveNewConfig")
    (action "saveNewConfigName")

}}
